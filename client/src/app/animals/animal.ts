export interface Animal{

        id: number;
        name: string;
        sexe: string;
        destinctiveSign: string;
        checkinDate: string;
        checkoutDate: string;
        pool: string;
        species: string
}